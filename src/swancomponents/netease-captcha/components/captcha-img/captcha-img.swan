<image class="image-class captcha-img" s-if="{{url}}" src="{{url}}" mode="{{mode}}" preview="false" bindload="handleLoaded" binderror="handleError" style="{{ imgStyle + (autoWidth ? 'width: ' + width + 'px;' : '') }}"></image>